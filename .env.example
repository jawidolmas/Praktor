# Where the supervisor keeps its state (SQLite file + checkpoints + worktrees)
EXEC_HOME=./.exec

# Worker concurrency. 1 on a Claude Pro subscription; raise on Max or with an API key.
EXEC_MAX_WORKERS=1

# Telegram control surface (apps/telegram)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
