{
  "version": "0.2.1",
  "asOf": "2026-09-18",
  "features": [
    "Plain-English intake — `exec-agent do \"...\"` resolves the repo and infers a check, or hands off to the planner",
    "Multi-task decomposition — one brain call breaks an objective into a dependency-ordered task graph",
    "Per-objective failure policy — escalate | abandon | skip when a task exhausts its attempts",
    "Persistent supervisor daemon — closing the terminal, or the laptop, does not stop the work",
    "git worktree isolation — a dedicated branch per attempt, nothing runs against your working tree",
    "Policy engine — denies or asks about force-push, prod deploys, destructive SQL, committed secrets",
    "Verification gate — the supervisor runs your real acceptance command; a worker's own \"done\" is advisory",
    "Independent judge — reviews each attempt's diff against intent before it counts as done, so a worker's own \"done\" is never the final word",
    "Cause-aware recovery — a failure diagnoser classifies flaky, bug, spec, or environment, and picks retry, retry-with-hint, respawn, or escalate",
    "Checkpoint & respawn — a fresh worker is seeded with what was tried and ruled out, not a longer chat",
    "Rate-limit park-and-resume — retried in place once the limit clears, never burns the attempt budget",
    "Standing engineering profile — preferences that shape every planning, review, and diagnosis call, plus a daily Telegram digest",
    "Decisions answerable anywhere — terminal, `exec-agent decide`, the dashboard, or Telegram",
    "Human approval gate — a diff per branch and a Merge button; nothing is ever merged automatically",
    "Full event log per objective — `exec-agent events <id>`, replayable, the actual source of truth",
    "Daemon autostart on login/reboot — with visibility in the dashboard",
    "Dashboard attribution — which model did the work, what the judge and diagnoser decided, and who answered an escalation and through which channel",
    "Local knowledge-graph tool — graphify builds a per-repo call graph with zero LLM tokens; the worker, judge, and diagnoser query it via MCP instead of re-grepping raw files on every attempt",
    "Dashboard knowledge-graph panel — node/edge/community counts, an extracted/inferred/ambiguous confidence breakdown, and the most-connected code per objective"
  ],
  "log": [
    {
      "date": "2026-09-18",
      "entries": [
        "Released `v0.2.1` — the worker, the independent judge, and the failure diagnoser can now query a local graphify knowledge graph via MCP instead of grepping raw files, and the dashboard shows it",
        "Gave the worker a second MCP server backed by graphify — a local, LLM-free per-repo call graph — with `query_graph`/`get_neighbors`/`shortest_path`/`god_nodes` preferred over Grep/Read for architecture questions",
        "Gave the independent judge and the failure diagnoser the same graph access — the judge checks blast radius (a changed function breaking a caller the acceptance checks never exercise), the diagnoser checks whether a failure's real cause sits outside the attempt's own diff",
        "Added a \"Knowledge graph\" panel to the dashboard's objective page — node/edge/community/file counts, a confidence breakdown, and the most-connected code by degree"
      ]
    },
    {
      "date": "2026-09-17",
      "entries": [
        "Released `v0.2.0` — the independent judge, cause-aware recovery, the engineering profile, and the redesigned dashboard, all together",
        "Redesigned the supervisor dashboard around attribution: a worker → judge → diagnoser pipeline per attempt, a full decision audit log with who-answered-how, cost and token rollups by model, and a health & auto-recovery view",
        "Added a standing engineering profile and a daily Telegram digest — what ran, what needs a decision, what recovered on its own"
      ]
    },
    {
      "date": "2026-09-16",
      "entries": [
        "Added an independent judge that reviews each attempt's diff against intent before it counts as done",
        "Added a cause-aware failure diagnoser (flaky, bug, spec, or environment) that decides what happens next, instead of a blind respawn",
        "Rebuilt \"how it works\" as a live pipeline graph, and added this changelog feed to the site",
        "Rebalanced the site's color theme — light by default, the brand blue now a deliberate accent",
        "Fixed a real CRLF conflict in the integration-branch fold, found live",
        "Fixed \"logon\" autostart never actually launching the daemon",
        "Fixed dependent-task blindness, approve races, and native merge confirms"
      ]
    },
    {
      "date": "2026-09-15",
      "entries": [
        "Dashboard now shows which branch failed to merge, and why — not a generic alert",
        "Fixed the \"--mode logon|boot\" ambiguity in both the CLI's output and the dashboard",
        "Fixed a 1.4s blocking call on the dashboard's hottest endpoint",
        "Fixed multi-task objectives only merging one branch on approve"
      ]
    },
    {
      "date": "2026-09-12",
      "entries": [
        "Added daemon autostart-on-reboot, with dashboard visibility",
        "Acceptance checks now run through Git's bundled bash, not cmd.exe",
        "Worktree checkouts made immune to the host's core.autocrlf",
        "Added an objective-level completion report instead of one task's story",
        "Fixed abandon leaving unrelated sibling tasks orphaned forever",
        "Fixed stale L3 decisions being replayed forever, ignoring later answers"
      ]
    }
  ]
}
